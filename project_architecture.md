# ארכיטקטורת מערכת: ענן המחשבות (Cloud of Thoughts)

מסמך זה מתאר בצורה מקיפה ומעמיקה את ארכיטקטורת הפרויקט "ענן המחשבות", בגרסתה העדכנית, המבוססת על הפרדה ברורה בין צד לקוח (Client) לצד שרת מרכזי (Shared Backend). המערכת שמה דגש על יישום מתודולוגיית ה-OKF (Obsidian Knowledge Folder) וניהול המידע במערכת.

## 1. ארכיטקטורה כללית (High-Level Architecture)

המערכת בנויה בארכיטקטורת Client-Server, שבה האפליקציה הנוכחית משמשת כ"לקוח רזה" (Thin Client) לתקשורת ותצוגה, בעוד שכל כובד המשקל האנליטי והקוגניטיבי מבוצע בשרת מרכזי (המשותף למערכת זו ולאפליקציה נוספת).

### חלוקת התפקידים המרכזית:
- **צד הלקוח (האפליקציה הנוכחית)**: אחראי אך ורק על איסוף הקלט מהמשתמש (הקלטת קול), תמלול השיחה, שליחת התמלול לשרת, והצגת התובנות שחוזרות מהשרת. **הלקוח אינו מייצר תובנות בעצמו**.
- **השרת המרכזי (Shared Server)**: מקבל את התמלולים הגולמיים מהאפליקציות השונות, מבצע ניתוח עומק מבוסס AI, מחלץ את התובנות על פי מתודולוגיית OKF, ומאחסן/מנגיש אותן חזרה ללקוח.

## 2. מנוע האנליזה המרכזי בשרת (Backend AI Engine)

השרת הוא המוח האנליטי של המערכת. הוא מופקד על בניית ה-Knowledge Graph וניהול מודל הנתונים:
על פי חוקי הפרויקט, השרת מייצר מידע התואם לשיטת OKF (Obsidian Knowledge Folder) ממבנה של צמתים (Nodes) וקשתות (Edges).

- **Triples (שלשות)**: השרת מפרק כל משפט או תמלול לשלשה של `Subject -> Relation -> Object` (לדוגמה: "אני -> מפחד מ -> כישלון"). 
- **Topics & Themes**: זיהוי אוטומטי של נושאי ליבה שחוזרים על עצמם ביומני המשתמש.
- **Moods & Emotional Momentum**: בניית היסטוריה רגשית ומעקב אחר המומנטום הרגשי לאורך זמן.
- **Socratic Persona (פרסונה וסתירות)**: השרת מאתר סתירות בדיווחים לאורך זמן או שינויים מגמתיים בחיי המשתמש כדי לייצר תובנות הבהרה עמוקות.

## 3. שכבת הלקוח (Client Application)

האפליקציה הנוכחית בנויה כ-SPA מבוססת React ו-Vite. תפקידה הוא להיות הממשק למשתמש ולאסוף את המידע:

- **איסוף ותמלול**: האפליקציה מקליטה את קול המשתמש, דואגת לתמלול מדויק של הטקסט, ומעבירה את הקובץ או הטקסט הגולמי לשרת באמצעות API.
- **קריאת נתונים בלבד (Read-Only Insights)**: לאחר שהשרת מנתח את התמלול, צד הלקוח קורא את התובנות המוכנות (JSON של שלשות, נושאים ומצבי רוח) ממסד הנתונים או מה-API.
- **ממשק תלת-ממדי (3D Constellation)**: הלקוח משתמש ב-Three.js (עם `@react-three/fiber`) כדי לקחת את התובנות שהשרת ייצר ולהציג אותן ויזואלית למשתמש כ"גלקסיית מחשבות" מרחבית.
- **VoicePulse**: ממשק ויזואלי להקלטת הקול שנותן פידבק בזמן אמת על דיבור המשתמש.

## 4. ניהול מצב ואחסון במסוף (State & Persistence)

מכיוון שהלקוח לא מבצע את הניתוח אלא רק מציג אותו, ניהול המצב בלקוח מתמקד בסנכרון מול השרת:

- **Zustand (`src/store.ts`)**: מנהל את המצב הגלובלי המקומי באפליקציה. הוא קורא ושומר את התובנות (ה-Triples וה-Insights) שהשרת החזיר, כדי לאפשר חווית דפדוף מהירה בצד הלקוח.
- **Firebase / Database Sync (`FirebaseStorageService.ts`)**: הלקוח מסתנכרן מול מאגר הנתונים (Firebase Firestore או Google Drive) כדי לקרוא את רשומות הידע המעודכנות (`entities`, `insights`, `entries`) שהשרת כתב, ולהציגן למשתמש. התהליך מתבסס על פונקציות טעינה (כמו `loadKnowledgeGraph` ו-`loadInsights`) שמביאות את התוצרים המעובדים.

## 5. זרימת הנתונים במערכת (Data Flow)

כדי להבין במדויק את חלוקת האחריות במערכת המעודכנת, להלן תרחיש הפעולה:

1. **קלט (Input)**: המשתמש מדבר אל האפליקציה (הלקוח הרזה).
2. **תמלול (Transcription)**: האפליקציה מקליטה את השמע, מתמללת אותו לטקסט ושומרת את הקלט הגולמי.
3. **שליחה (Transmission)**: האפליקציה שולחת את התמלול (Transcript) לשרת המרכזי באמצעות API/Firestore.
4. **ניתוח ענן מרכזי (Server-Side Analysis)**: השרת מקבל את המידע, מפעיל את מודלי ה-AI (Gemini), שולף Triples ונושאים (OKF), מחשב מומנטום רגשי, ומעדכן את בסיס הנתונים (Firestore / ענן).
5. **קריאה לתצוגה (Read & Display)**: האפליקציה קוראת בחזרה את הנתונים המנותחים מהשרת ומשקפת אותם למשתמש (באמצעות ה-Constellation התלת-ממדי, הצגת התובנות, ורשימת הנושאים).

---
מסמך זה משקף את הארכיטקטורה המדויקת שבה מבוצעת הפרדה מוחלטת: האפליקציה המקומית מתמקדת בחווית המשתמש, הקלטה ותצוגה – והשרת המשותף מתמקד באנליזה וייצור הידע המובנה.
